/* 
* The difference between tail recursion and Nontail
* The processing occurs before the recursive call.
* There is just the function of tail recursive 
*/
public void tail(int n)
{
   if(n == 1)
      return;
   else 
      System.out.println(n);

   tail(n-1);
}
